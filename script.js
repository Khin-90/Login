// Placeholder for adding interactivity if needed
document.querySelectorAll(".google-login").forEach(button => {
    button.addEventListener("click", () => {
        alert("Google login feature not implemented yet!");
    });
});
